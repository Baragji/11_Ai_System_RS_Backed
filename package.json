{
  "name": "ai-platform-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "apps/api"
  ],
  "scripts": {
    "lint": "npm run lint --workspace=apps/api",
    "build": "npm run build --workspace=apps/api",
    "start": "npm run start --workspace=apps/api"
  }
}
